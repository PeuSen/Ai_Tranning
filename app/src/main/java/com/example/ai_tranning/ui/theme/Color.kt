package com.example.ai_tranning.ui.theme

import androidx.compose.ui.graphics.Color

/** Light purple primary used in the dark color scheme. */
val Purple80 = Color(0xFFD0BCFF)
/** Light purple-grey secondary used in the dark color scheme. */
val PurpleGrey80 = Color(0xFFCCC2DC)
/** Light pink tertiary used in the dark color scheme. */
val Pink80 = Color(0xFFEFB8C8)

/** Deep purple primary used in the light color scheme. */
val Purple40 = Color(0xFF6650a4)
/** Deep purple-grey secondary used in the light color scheme. */
val PurpleGrey40 = Color(0xFF625b71)
/** Deep pink tertiary used in the light color scheme. */
val Pink40 = Color(0xFF7D5260)

// --- TaskCo design-system palette (Tailwind-equivalent tokens) ---

/** Primary action color — Tailwind `blue-600`; used for primary buttons and the IN_PROGRESS status. */
val BluePrimary = Color(0xFF2563EB)
/** Danger color — Tailwind `red-600`; used for destructive actions (delete) and the HIGH priority. */
val DangerRed = Color(0xFFDC2626)
/** App background — Tailwind `gray-50`. */
val Gray50 = Color(0xFFF9FAFB)
/** Card / container surface — white. */
val SurfaceWhite = Color(0xFFFFFFFF)
/** Title text — Tailwind `gray-900`. */
val Gray900 = Color(0xFF111827)
/** Body text — Tailwind `gray-600`. */
val Gray600 = Color(0xFF4B5563)

/** Status TODO background/foreground — gray. */
val StatusTodoBg = Color(0xFFE5E7EB)
val StatusTodoFg = Color(0xFF374151)
/** Status IN_PROGRESS background/foreground — blue. */
val StatusInProgressBg = Color(0xFFDBEAFE)
val StatusInProgressFg = Color(0xFF1D4ED8)
/** Status DONE background/foreground — green. */
val StatusDoneBg = Color(0xFFD1FAE5)
val StatusDoneFg = Color(0xFF047857)